http {
    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html;
        index index.html;

        location /stub_status {
            stub_status;
            access_log off;
            allow 127.0.0.1;   
            deny all;
        }
    }
}